<h1> Hello Admin <%= current_user.name %> </h1>
<table class="table"> 
  <thead> 
    <tr> 
      <td> # </td>
      <td> ID </td>
      <td> 名稱 </td>
      <td> 是否管理者 </td>
    </tr>
  </thead>
  <tbody> 
  <%= render partial: 'user_list', collection: @users, as: :user %>
  </tbody>
</table>
