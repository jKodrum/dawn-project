<% content_for :search_body do %>
  <table class="table table-striped"> 
    <thead> 
      <tr> 
        <td> # </td>
        <td> ID </td>
        <td> 名稱 </td>
        <td> 狀態 </td>
      </tr>
    </thead>
    <tbody> 
      <%= render partial: 'users/volunteer_list', collection: @volunteers, as: :volunteer %>
    </tbody>
  </table>
<% end %>

<%= render 'search' %>
