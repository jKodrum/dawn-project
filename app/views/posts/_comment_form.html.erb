<%= bootstrap_form_for [@post, @comment], url: post_comments_path(@post.author, @post.author, @post, @comment) do |f| %>
  <%= current_user_image_tag %>
  <%= user_tag %>
  <%= f.text_field :content, hide_label: true, placeholder: "留言...", icon: "pencil" %>
  <%= f.submit "留言" %>
<% end %>
